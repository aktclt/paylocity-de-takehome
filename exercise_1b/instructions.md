# Exercise 1b

## Requirements Change!!

It turns out that the source team cannot send you the events they promised earlier!

So instead they sent you the full data when a new employee is created, then only partially hydrated events for all updates. Where only the columns that change are provided, the rest is nulled out. For deletes you only get the id!

How do you handle this? Making sure to still fulfill the business requirements.
